--- watchdog.sh.upstream-20260302.example	2026-03-12 10:58:40
+++ watchdog.sh.patched.example	2026-03-12 10:58:39
@@ -146,7 +146,7 @@
 			# Remote display host (e.g. nowplaying.local:8101) does not expose /command/.
 			# Query now-playing API state instead and wake while Now Playing reports active playback.
 			REMOTE_HOST=${LOCAL_DISPLAY_HOST%%:*}
-			NP_STATE=$(curl -s --max-time 1 "http://$REMOTE_HOST:3000/now-playing" | awk -F'"state":"' '{print $2}' | awk -F'"' '{print $1}')
+			NP_JSON=$(curl -s --max-time 1 "http://$REMOTE_HOST:3101/now-playing")
+			NP_STATE=$(printf '%s' "$NP_JSON" | awk -F'"state":"' '{print $2}' | awk -F'"' '{print $1}')
+			NP_ACTIVE=$(printf '%s' "$NP_JSON" | grep -E -c '"isAirplay":true|"isUpnp":true')
 			[ "$NP_STATE" = "play" ] || [ "$NP_ACTIVE" != "0" ] && HW_PARAMS="playing" || HW_PARAMS="closed"
 		fi
 
