// src/routes/config.routes.mjs
// Legacy container kept for compatibility; routes have been extracted into focused modules.

export function registerConfigRoutes(_app, _deps) {
  // Intentionally no-op.
}
